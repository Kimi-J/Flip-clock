# 叶落成时 / FALLING TIME — 技术设计文档

> 皮肤三号。设计纲领只有一句:**时间不是"显示出来",而是被一片落叶"换出来"。**
> 机械台钟:翻页换时间。票根:印刷记录时间。落叶:飘落揭示时间。

本文档只出设计,不含代码。目标读者是实施者(未来的我),因此每个设计点都落到
"用什么技术、在哪个文件、什么数据流、什么边界条件"。

---

## 1. 接入点(与既有皮肤系统的关系)

现有皮肤系统已经是"壳层 + 皮肤组件"结构,本皮肤是一个纯增量:

| 位置 | 改动 |
|---|---|
| `src/store/clockStore.ts` | `WidgetSkinName` 增加 `"falling"`;`WIDGET_SKIN_OPTIONS` 追加 `{ value: "falling", label: "叶落成时" }` |
| `src/pages/WidgetPage.tsx` | `SKIN_BASE` / `SKIN_LIMITS` 增加 falling 条目;渲染分支增加 `<FallingSkin />` |
| `src-tauri/src/lib.rs` | `SkinGeom` 增加 `"falling"` 分支(base/limits);`set_widget_skin` 校验白名单加 `"falling"` |
| 新增 `src/pages/widget/FallingSkin.tsx` | 皮肤主组件(场景编排) |
| 新增 `src/pages/widget/fallingPhysics.ts` | 落叶动力学 + 路径生成(纯函数,可单测) |
| 新增 `src/pages/widget/falling.css` | 皮肤样式。**不并入 index.css**:本皮肤样式量预计超过 index.css 现有全部皮肤之和;独立文件,直接静态 import(体积小,代码分割的收益不抵异步加载的复杂度) |

窗口几何建议:

- 基准窗口 **320×180**(内容场景 280×140,四边 20px 留白供投影/叶片飞出余量)
- 长宽比锁定 320:180(16:9),min 256×144,max 640×360
- 透明窗口三件套、手动拖拽、中心锚定缩放、wmenu 菜单——**全部复用壳层,零新增**

壳层有一个现成红利:整个场景在 `.falling-wrap` 里被 `transform: scale(s)` 等比缩放,
所以**所有内部坐标都可以按基准分辨率(320×180)的 CSS 像素写死**,遮挡换算、路径生成
完全不感知缩放。

---

## 2. 场景分层(z-index 从后到前)

空间感来自"前玻璃—数字—枝条—叶堆"的真实纵深,分层是实现它的根基:

```
z5  玻璃层      极弱高光渐变 + 鼠标视差高光(前景,pointer-events: none)
z4  前景落叶层   飘落中的叶子(在数字之前经过,产生遮挡)
z3  叶影层       叶子的"投影"(mix-blend-mode: multiply 只压在数字上)
z2  数字层       大号时间数字(墨褐)+ SEP 19 · SAT
z1  场景层(SVG)  背景骨白底、枝条、枝头叶(含目标叶)、底部落叶堆
```

要点:

- **数字必须是 DOM 文本层**(不是 SVG text):字体渲染、抗锯齿、`mix-blend-mode` 接收叶影都更稳。
- 飘落叶子是**独立 DOM 元素**(一张银杏 SVG 内联),`transform` 走 GPU 合成,路径由 rAF 驱动。
- 叶影是另一张同形 SVG,`filter: blur()` + `multiply` 混合,只出现在数字层上方(z3),所以影子"只落在数字和背景上,不浮在叶子上"。
- 玻璃层是线性高光 + 一个跟随鼠标的径向高光(slow mouse = 视差),不拦截任何事件。

---

## 3. 时间模型:一分钟被编排成一条时间轴

这是整个皮肤的调度核心。**离散事件走 React state,连续动画走 rAF + ref 直写 DOM**——
rAF 每帧 setState 会把 React 拖垮,这是性能红线。

### 3.1 时间源

不用 `useClockTime` 的秒级粒度。皮肤内部自建 rAF 循环,每帧取:

```
t = Date.now()
secOfMinute = (t / 1000) mod 60        // 0.000 ~ 59.999,浮点
minuteStamp = floor(t / 60000)         // 分钟编号,用于触发换分
```

对齐误差无要求(不像全屏翻页要贴秒边界),因为"59 秒脱离"本身就是表演的一部分。

### 3.2 一分钟的阶段状态机

| 阶段 | 秒区间 | 内容 | 实现 |
|---|---|---|---|
| 静息 | 00.0–35.0 | 枝叶微动(空气感),叶影极弱移动 | 见 §5 风场 |
| 衰老 | 35.0–50.0 | 目标叶从叶柄向叶尖褪色、微卷、叶脉变深 | 叶色 = 两个渐变层 opacity 插值,进度 = (sec-35)/15;卷曲面用 `rotateX` 微量 + 路径形变(SVG `d` 两套插值或 scaleY 扰动) |
| 风强 | 50.0–57.6 | 全局风场强度 ramp up,其他叶摆动加剧,目标叶柄部摆动幅度单独加大 | wind 系数 = 1 + (sec-50)/7.6 × 0.8 |
| 脱离 | 57.6 | 叶柄"断开":目标叶从枝头 SVG 摘除,注册为前景落叶层元素,初始速度 = 当前摆动速度(连续性) | 57.6 时刻一次性生成落叶对象(见 §4) |
| 飘落 | 57.6–60.0+ | 叶子沿生成路径下落,**覆盖窗口 [59.85, 60.15] 经过分钟个位数字** | rAF 驱动 transform |
| 换数 | **60.0(分钟边界)** | minuteStamp 翻转即换值,全程处于叶片覆盖窗口内 | 见 §6 |
| 落堆 | ~60.5+ | 叶子注册进落叶堆(回到场景层 z1),前景元素销毁 | 见 §7 |

**换数必须钉死在真实分钟边界(60.0s),不许抢跑**:覆盖窗口 [59.85, 60.15]
对称跨过边界,数字在完全遮挡中翻转。若提前到 59.x 换数,时钟会提前几十厘秒
显示下一分钟——对时钟产品这是事实错误,遮挡掩护不能成为抢跑的理由。
数字事件时间驱动(边界必换),路径被约束生成(边界必遮住)——
下一节的动力学系统就是为此服务的。

---

## 4. 落叶动力学系统(fallingPhysics.ts)

### 4.1 设计原则:目标驱动 + 拒绝采样,而非自由模拟

自由前向模拟(重力/阻力/风随机给)无法保证"59.2s 恰好遮住个位数字"。
反过来做:**先定约束,再生成路径**。

输入:

- `start`:目标叶在枝头的位置(枝头叶的锚点,场景坐标)
- `coverPoint`:分钟个位数字的中心点(数字层布局后由 `getBoundingClientRect` 换算进场景坐标;12/24 制、显秒开关不影响——个位分钟永远存在)
- `coverTime`:60.0s(常量,分钟边界);覆盖窗口 [59.85, 60.15]
- `slot`:本分钟落叶堆的目标槽位(见 §7)
- `seed`:分钟编号(随机性可复现,便于调试;同一分钟重来路径一致)

生成步骤:

1. **骨架**:start → coverPoint → slot 三点定一条下落基线(两段:先摆荡下降,过数字后加速落向槽位)。
2. **横向摆动**:`x(t) = baseline(t) + A1·sin(ω1·t+φ1) + A2·sin(ω2·t+φ2) + noise(t)`,
   A/ω/φ 由 seed 决定;约束求解:调整 φ1 使 `x(coverTime) = coverPoint.x`(一维求根,几步牛顿迭代即收敛)。
3. **竖向速度**:分段 easing(脱离瞬间继承摆动速度 → 自由加速 → 终端速度),保证 `y(coverTime) = coverPoint.y`。
4. **自旋与翻面**:`rotate(t)` 慢速进动;`scaleX(t) = cos(flip(t))` 模拟 Z 轴翻转,
   过零时切换正/反面贴图(银杏正反面明暗两套填充)。翻面频率、相位由 seed 决定。
5. **风格骰子**(由 seed 决定本分钟气质):
   - 70% 常规:摇摆下落
   - 15% 风卷:路径中段加一个上升环(y 回抬 20–40px)
   - 10% 直落横滑:近垂直,落地前横向滑出
   - 5% 气流托停:中段速度骤降(悬停 ~1s)再继续——**总时长会超出 60.0s,允许**;
     但悬停只允许发生在**通过 coverPoint 之后**(悬停在数字前方会拖延覆盖窗口,
     破坏边界对齐);落堆晚于整分钟边界,与下一片叶的生成不冲突(见 §4.3)

### 4.2 遮挡保证(关键工程点)

生成后自检:覆盖窗口 [59.85, 60.15] 内叶片包围盒与数字包围盒的**面积覆盖率
全程 ≥ 85%**(叶片尺寸取数字字框的 ~1.25 倍,银杏横置时宽约 70px,足够罩住个位数字)。

不满足则以 `seed+1` 重试(最多 8 次);仍不满足的兜底:把基线中段硬约束为过
coverPoint 的直线(牺牲飘逸感,保证事件)。**数字换值永远不等叶子**——由
minuteStamp 在 60.0s 翻转直接驱动,遮挡只是视觉掩护,不是逻辑依赖;
即使 8 次重试全部失败,数字也只是在边界"裸换"一次,逻辑零错误。

### 4.3 并发与目标叶再生

- 同一时刻最多一片"飘落中的叶子"。下一片在下一分钟 57.6s 脱离;若上一片因
  气流托停还没落堆,则下一片照常脱离,两片并存(视觉上完全自然)。
- **目标叶再生(反直觉但必需)**:一小时落 60 片,枝头只有 5–8 片装饰叶,
  不做再生三分钟就薅秃了。因此装饰叶永不掉落;每分钟的"目标叶"是独立元素,
  在 **00.0s 于枝头末端淡入**(2s,opacity 0→1,混在其他叶间几不可察),
  随后经历衰老/风强/脱离的完整生命周期。用户的注意力被风场和叶堆占据,
  淡入不会穿帮——这是"每分钟一片叶"的诚实工程解。
- 皮肤挂载/换肤切入时:若当前 secOfMinute > 35,目标叶直接以"衰老进度"静态呈现;
  > 57.6 则跳过本分钟落叶(避免半路空降),落叶堆按"当前分钟数"一次性铺好(见 §7)。

---

## 5. 环境风场与枝叶微动(00–57.6s 的"活着"感)

反对循环播放 `rotate(-2°)→rotate(2°)` 的关键是**多相位 + 传播延迟**:

- 枝头 6–10 片叶,每片有独立的 `sway(freq, phase, amp)`(seed 固定,不随时间变)。
- 枝条本身是一个低频大摆,叶片相位 = 枝条相位 + 距离延迟(叶柄晚 ~40ms,叶尖再晚 ~30ms)。
  实现:每片叶的摆动角 = `branchSway(t - delay_i) × gain_i + leafSway_i(t)`。
- 全部在 rAF 里计算后直接写 `element.style.transform`,无 setState。
- 玻璃上的叶影:场景层叶子的 blur 副本,`opacity ≤ 0.06`,随摆动位移 ×0.3。

**鼠标风场**(悬停惊艳项):

- `pointermove` 记录速度向量,注入风场 `wind.v += v × k`,风场自身带指数衰减。
- 响应按距离传播:每片叶/枝的响应延迟 = `dist(leaf, cursor) / speed`,强度随距离衰减。
- 地面叶堆:近处叶子被推动位移 1–3cm(弹簧回位)。
- **铁律:目标叶(代表当前分钟)不可被拨掉**。它可以被吹得疯狂摆动,摆动物理照常,
  但脱离只发生在 58.0s。这个"用户可以逗它但改变不了时间"的性格必须在代码里显式守住。

---

## 6. 数字换出机制(核心设计点)

```
57.6s   叶脱离枝头,开始下落(前景层 z4)
59.85s  进入覆盖窗口:叶面积覆盖个位数字 ≥85%(影子此时最锐:z≈1,blur 最小)
60.0s   minuteStamp 翻转 → React 换值(分钟 +1),处于完全遮挡中
60.15s  叶离开数字区,覆盖窗口结束
~60.5s+ 落堆
```

用户看到的时间线:`04` → 叶子飘过(影子先压上数字)→ `05`。
**换值发生在真实分钟边界、且被叶片完全遮挡,既准确又不可见。**

细节:

- 叶影(z3)的锐度映射虚拟深度:`blur = 8×|z-1| px, opacity = 0.35×(1-|z-1|)`,
  z 从 0(玻璃)到 1(数字平面)到 1.6(叶堆平面)。叶子贴近数字平面时影子最锐——
  这就是"二维窗口突然有空间感"的来源。
- 换数用现成的状态模式:`minuteStamp` 变化 → React 重渲染数字层(静止文本,无动画)。
  若遮挡自检兜底失败(极端情况),数字在边界裸换,视觉损失可接受,逻辑绝不错误。
- 12 小时制:数字层换值逻辑与全屏共享 `useClockStore().is24Hour`,遮挡目标始终是
  "分钟个位",与制式无关。
- `coverPoint` 换算:`getBoundingClientRect` 读到的是**缩放后**的屏幕坐标,
  必须除以壳层当前 `scale` 再减去 wrap 偏移,才是场景基准坐标——缩放拖拽后
  数字位置不变(场景内部相对坐标恒定),但每次换肤/12h 切换/显秒切换都要重读。

---

## 7. 落叶堆 = 分钟进度条

> 一分钟是一片叶子。一小时是一阵风。

- **槽位制,不做刚体碰撞**:预设 60 个槽位的确定性伪随机布局(种子固定):
  x 以中右部为重心聚集、y 两层错落、每槽 rotation/scale/正反随机。
  第 N 分钟落叶落向第 N 槽。叶片间遮挡靠槽位 z 序(先落在下)天然形成。
- 落堆瞬间:1.5 倍速的 100ms "贴地"沉降(scaleY 0.9 → 1 轻弹)+ 阴影定格为最软。
- 读法:用户凭堆量感知小时进度。**静止时叶堆零动画**(性能)。
- 皮肤中途挂载:按当前分钟数直接铺 N 槽,无动画。

### 整点大事件(10:59 → 11:00)

编排(总时长 ~2.2s):

1. `~00.5s`(新小时,下同):第 60 片落堆,叶堆完整。静默 0.5s(风场归零,万籁俱寂——反差感)。
2. `~01.0s`:左侧来风,wind 冲量 ×6。叶堆**从最上层(后落的)开始**依次被卷起:
   每片叶延迟 = 槽位 x 坐标从左到右映射 0–600ms(风有传播速度)。
3. 卷起路径三类(按槽位 seed 分配):贴玻璃滑出(前景 z4,大 blur)、旋转飞向
   前景(scale 到 1.4 再出画)、从数字后方掠过(z 介于数字与场景之间)。
4. 彩蛋:随机一片叶"啪"贴上玻璃(scale 1.15 + blur 0 + 静停 300ms),再被风带走。
5. `~02.7s`:全部出画,叶堆清空(`pile = []`),场景回到新的一小时。

实现:叶堆元素本身是静态 DOM,风起时批量转交 rAF 编排器(每片一条迷你路径),
结束即销毁,回归空数组。

---

## 8. 秒针 = 叶脉水分

- 目标叶(枝头,将落的那片)是一张银杏 SVG,内含主叶脉 `<path>`(叶柄→叶尖)。
- 两条同形叶脉叠加:底层"枯萎色"(枯褐),顶层"鲜活色"(青绿→鹅黄)。
- **采用"水分渐退"(已定稿)**:00s 时顶层鲜活色完整覆盖整条叶脉;随秒数推进,
  鲜活色**从叶尖向叶柄回缩**(叶片失水总是从尖端开始)——`dasharray = L`,
  `dashoffset` 取负值使可见段起点向叶柄方向收缩,`offset = -L × secOfMinute/60`。
  59s 时整条枯萎,随即断柄落下。**这是每秒都在走但完全不显式读秒的秒针**。
  (`L` 在挂载时由 `getTotalLength()` 实测;SVG 内部坐标,不受壳层 scale 影响)
- 注意与衰老期(§3.2)的方向差异是刻意的:整体褪色从叶柄向叶尖(颜色褪去),
  叶脉失水从叶尖向叶柄(水分退出)——一个管"色",一个管"水",互不冲突。
- 秒数显示开关(`widgetShowSeconds`)对本皮肤的语义:开关 ON 时在日期行追加小字
  秒数(照顾想要精确读秒的用户);OFF 时叶脉即秒(默认,推荐)。

---

## 9. 静态 UI 与视觉规范(极度克制)

动画已经很满,静态部分必须退后:

```
FALLING TIME                    ← 9px,letter-spacing 3px,枯褐 60% 透明

   10:04                        ← 占场景高 55–60%,墨褐 #3a2f28(非纯黑)

SEP 19 · SAT                    ← 10px,同色系更浅
```

- 背景:偏暖骨白 `#f4f0e8` → 极浅灰褐 `#e8e2d6` 的柔和径向渐变(Shadow Box 内壁感)。
- 银杏叶:扇形单叶 SVG,正面鹅黄绿渐变 + 叶脉纹,反面明度降 15%;轮廓手写 path
  (不要用素材库枫叶)。枝头叶 5–8 片,大小 24–40px 错落。
- 玻璃层:左上 15% 方向的线性高光(opacity 0.05)+ 鼠标慢移时的视差高光。
- 阴影:窗口投影沿用壳层规范(drop-shadow,范围 < 20px 留白)。

---

## 10. 性能预算

| 项 | 预算 | 手段 |
|---|---|---|
| 常驻 rAF | 1 个(皮肤级唯一循环) | 所有连续动画(风场/摆动/飘落/叶脉)都在这一循环内结算 |
| React 重渲染 | 每分钟 1–2 次 | 数字换值、落堆计数;连续量一律 ref 直写 DOM |
| 同时动画元素 | ≤ 12(枝叶)+ 1–2(飘落)+ 影 | 叶堆静止时零动画;整点风起例外(~60 片,2.2s,一次性) |
| 每帧样式写 | 仅 transform/opacity/dashoffset | 禁 width/height/filter 逐帧(影子的 blur 只在飘落期间变,可接受) |
| 合成层 | will-change 只给飘落叶 | 枝叶摆动不需要层提升(transform 已走合成器) |

WebView2 常驻常开,此预算下 CPU 占用应与票根皮肤同量级。

---

## 11. 风险与对策

| 风险 | 等级 | 对策 |
|---|---|---|
| 遮挡时机错位(叶子没遮住数字就换了) | 高(核心体验) | 目标驱动生成 + 覆盖率自检 + 拒绝采样 + 直线兜底;换值永不等叶子(§4.2) |
| ~~换数早于真实分钟边界(抢跑)~~ | 高(已修正) | 换数由 minuteStamp 边界(60.0s)驱动,覆盖窗口对称跨边界(§3.2/§4.2) |
| ~~枝头叶一小时被薅秃~~ | 高(已修正) | 装饰叶永不落;目标叶每分钟 00.0s 淡入再生(§4.3) |
| 逐帧 rAF 掉帧导致路径跳变 | 中 | 路径是 t 的纯函数(非积分累积),掉帧只丢中间姿态不丢轨迹 |
| 60 片叶堆 DOM 常驻 | 低 | 静止无动画;仅在风起时短暂动画;SVG 内联单片 < 1KB |
| 12h/显秒切换改变数字宽度,coverPoint 失效 | 中 | 数字层布局后(每帧廉价可读,或换值时重读)动态取个位数字 rect,不写死坐标;rect 须除以壳层 scale(§6) |
| 整点风起与"第 60 片落堆"重叠 | 中 | 风起延迟 0.5s 即为此设计;编排器串行,不并行 |
| mix-blend-mode 叶影层合成开销 | 低 | 影子层仅在飘落 ~2s 内存活,其余时间为空(不挂载) |
| 系统时间跳变(NTP 校准/手动改时间) | 低 | 时间源用 Date.now() 绝对时刻重算 secOfMinute,跳变表现为演出跳帧一次,可接受 |
| 用户挂机跨小时(屏幕锁定) | 低 | rAF 自然停摆,恢复后按当前分钟重铺叶堆(§4.3 挂载逻辑复用) |

---

## 12. 实施里程碑

- **M1 静态场景**:场景层 SVG(骨白底/枝条/枝头叶)+ 数字层 + 玻璃层接入壳层,
  换肤可用,无落叶逻辑。验收:皮肤可切、可拖、可缩放、数字正常走。
- **M2 落叶换数(核心)**:fallingPhysics 路径生成 + 目标叶脱离/飘落/遮挡换数 + 叶影深度。
  验收:盯着看一分钟,57.6s 叶子脱离,60.0s 边界数字在完全遮挡中换到新值(不早不晚)。
- **M3 叶堆与整点**:槽位叶堆 + 落堆沉降 + 整点风起清空(含贴玻璃彩蛋)。
  验收:跨整点观察一次完整"风起"。
- **M4 氛围层**:叶脉秒针 + 鼠标风场(含"目标叶不可拨掉"铁律)+ 玻璃视差 +
  衰老/风强阶段的过渡抛光。

M2 是整个皮肤的灵魂,建议先做细;M1 只是脚手架,不要在里面磨设计。
